# STATE

## Current

Git home is this repo. Copied from `ChromExtentionProjects/extensions/tiktok-seller-tool` plus TikTok backend slice.

## Backlog

1. Host `store/privacy.html` on HTTPS + CWS submit
2. Production domain, Stripe live keys, backend deploy
3. Playwright on live Seller Center (Phase 2)
4. Overlay drag (position setting exists; not drag)
5. Firebase Auth/Firestore (prompt); email JWT is current
6. Capture real 1280×800 overlay screenshot

## Done

- 2026-09-19: Transferred extension + prompt + `backend/` into Git

## Notes

Prompt §1 says Free 25 SKUs; §3/§10 and code use **10**. Keep 10.

`backend/main.py` still registers other ChromExtentionProjects services. TikTok slug is `tiktok-seller-tool`.
